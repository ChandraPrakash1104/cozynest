import { LoadingIcon } from '../../../utils/icons';

const Loader = () => {
  return (
    <div className='w-screen flex justify-center items-center min-h-36'>
      <LoadingIcon />
    </div>
  );
};

export default Loader;
